{
  "name": "fieldwork-website-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "postinstall": "cd server && npm install --legacy-peer-deps",
    "start": "cd server && npm start"
  }
}